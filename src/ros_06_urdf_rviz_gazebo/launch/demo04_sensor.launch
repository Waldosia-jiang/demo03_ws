<launch>

<!--.发布新设置的坐标系到kinect连杆的坐标变换关系-->
<node pkg="tf2_ros" type="static_transform_publisher" name="static_transform_publisher" args="0 0 0 -1.57 0 -1.57 /laser_support /laser_support_depth" />

<!--启动 rviz -->
<node pkg="rviz" type="rviz" name="rviz" args="-d $(find ros_06_urdf_rviz)/config/show_mycar.rviz"/>

    <!--
只有上述两条语句：
   表现：设置不显示
   提示：No tf data.  Actual error: Fixed Frame [base_link] does not exist
   原因：rviz中显示URDF时，必须发布不同部件之间的坐标系关系
   解决：ROS中提供了关于机器人模型显示的坐标发布相关节点（两个）
-->


    <!-- 添加关节状态发布节点 --> 
    <node pkg="joint_state_publisher" type="joint_state_publisher" name="joint_state_publisher"/>
    <!-- 添加机器人状态发布节点 -->
    <node pkg="robot_state_publisher" type="robot_state_publisher" name="robot_state_publisher"/>
    
  
</launch>